# Run
To run the module in repl use command: futhark repl JSON-module.fut 

# benchmarks
To run benchmarks with multicore c backend use the command: futhark bench --backend=multicore JSON-benchmark.fut

To run benchmarks with c backend use the command: futhark bench --backend=c JSON-benchmark.fut